{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": "Host=100.79.128.25;Port=5432;Database=saas_dev;Username=postgres;Password=qiand68+++"
  },
  "JWT_SIGNING_KEY": "dev-key-32-bytes-minimum-length!",
  "JWT_ISSUER": "saas-identity-platform",
  "JWT_AUDIENCE": "saas-identity-platform-clients",
  "JWT_AUTHORITY": "https://auth.example.com",
  "SAAS_CORS_ALLOWED_ORIGINS": "http://localhost:3000,http://localhost:5173,http://localhost:3001",
  "_comment": "Phase 4 env 对称化: 顶层 flat key (Jwt:SigningKey 等 → JWT_SIGNING_KEY flat; Saas:Cors:AllowedOrigins → SAAS_CORS_ALLOWED_ORIGINS)。ASP.NET Core 默认 env provider 把 JWT_SIGNING_KEY env 直接读为 flat config key, 不走 `:` 段映射。镜像 saas-springboot @Value(\${JWT_SIGNING_KEY}) 与 saas-msw / saas-nextjs-self process.env.JWT_SIGNING_KEY。"
}